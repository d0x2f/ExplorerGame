#include "../globals.h"

#include "GLUnit.h"

GLUnit::GLUnit(Point pos, GLMesh *mesh, VideoInfo *video_info) : pos(pos), mesh(mesh), video_info(video_info) {
}

GLUnit::~GLUnit() {
}

void GLUnit::draw() {
	
}